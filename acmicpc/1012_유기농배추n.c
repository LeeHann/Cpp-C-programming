#include <stdio.h>

typedef struct
{
	int x, y;
}pnt;

pnt xy[2501];

int main()
{
	int t, i, m, n, k, j;
	scanf("%d", &t);
	for(i=0; i<t; i++)
	{
		scanf("%d %d %d", &m, &n, &k);
		for(j=0; j<k; j++)
		{
			scanf("%d %d", &xy[j].x, &xy[j].y);
		}
		if
	}
	return 0;
}
